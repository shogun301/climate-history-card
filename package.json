{
  "name": "climate-history-card",
  "version": "0.1.2",
  "description": "Home Assistant climate history card with continuous measurements, stepped setpoints, active-period shading, and recorded or unattributed setpoint changes.",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node --check src/climate-history-card.js && node --check dist/climate-history-card.js",
    "test": "node --test tests/*.test.mjs",
    "privacy": "node scripts/privacy-scan.mjs",
    "validate": "npm run build && npm run check && npm test && npm run privacy"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "Apache-2.0"
}
